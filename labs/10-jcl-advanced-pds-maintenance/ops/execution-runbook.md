# Execution Runbook
Prepare/rewrite members; COMPRESS the PDS; create PDS2; merge PDS + PDS2;
validate 3/3 and 2/2 copies; create RENAME; copy MEMBERD as RENAMED; validate
SDSF, SYSPRINT, member lists and member content.
