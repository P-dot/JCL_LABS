# Git Close - Lab 06

Ruta esperada:

```text
labs/06-jcl-sequential-dataset-delete
```

Commit recomendado:

```text
Complete Lab 06 sequential data set deletion lifecycle
```

Antes del push revisar `git diff --cached --name-status` y comprobar que solo
se incluye Lab 06.
